import { SettingsCardList } from "@/widgets/settings-card-list";

export const SettingsPage = () => {
  return (
    <main className="w-full max-h-[calc(100vh-80px)] h-full max-w-[1440px] mx-auto px-6">
      <SettingsCardList />
    </main>
  );
};
